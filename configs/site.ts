import { env } from '@/env.mjs';

export const siteConfig = {
  title: 'L2Pay.ing',
  description:
    'Keep paying with crypto. Reach new customers, scale your businesses with L2Pay. #l2pay #starknet #dojo',
  url: env.WEBSITE_URL,
};
